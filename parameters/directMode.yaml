name: directMode
description: Executes the operation directly without using other services.
in: query
required: false
example: true
deprecated: true
schema:
  type: string
  enum:
    - true
    - false