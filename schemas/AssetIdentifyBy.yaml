type: string
enum:
  - resourceId
  - deviceId
  - id
  - gai-siteId
x-enum-varnames:
  - ASSET_IDENTIFY_BY_RESOURCE_ID
  - ASSET_IDENTIFY_BY_DEVICE_ID
  - ASSET_IDENTIFY_BY_ID
  - ASSET_IDENTIFY_BY_GAI_SITE_ID
